<p><%= link_to "Create a new List", new_list_path %>
<ul>
	<% @lists.each do |list| %>
		<li><%= link_to list.name, list_path(list) %>, 
				<%= link_to "Edit", edit_list_path(list) %>,
		 		<%= link_to "Delete", list_path(list), method: :delete, confirm: "Are you sure?" %></li>
	<% end %>
</ul>